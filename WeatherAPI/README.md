# Weather API Consumer

Console app that fetches weather data from OpenWeatherMap API.

## Setup

1. Get API key from https://openweathermap.org/api
2. Set environment variable: `OPENWEATHER_API_KEY=your_key`
3. Run: `dotnet run`

## Usage

Enter city name when prompted. Type 'exit' to quit.
